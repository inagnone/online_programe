package 剑指offer;
import java.util.Scanner;

public class Tree {
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		String prestr,inostr;
		while(scanner.hasNext()){
			prestr = scanner.next();
			inostr = scanner.next();
			for(int i=0;i<prestr.length();i++){
				char root = prestr.charAt(i);
				int indexOfroot = inostr.indexOf(root);
				
			}
			
		}
	}
}
